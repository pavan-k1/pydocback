{"username": "user1", "filename": "generated_sample.py", "original": "\ndef add_numbers(a, b):\n    \"\"\"This docstring is just to confuse not a proper docstring.\"\"\"\n    return a + b\n\ndef is_even(number):\n    return number % 2 == 0\n\ndef factorial(n):\n    result = 1\n    for i in range(1, n + 1):\n        result *= i\n    return result\n\n", "updated": "\"\"\"This module provides utility functions for arithmetic operations.\n\nIt implements addition, even number checking, and factorial calculation.\n\"\"\"\n\ndef add_numbers(a, b):\n    \"\"\"Add two numbers.\n\n    Calculates the sum of two numeric inputs.\n\n    Args:\n        a (int | float): The first number.\n        b (int | float): The second number.\n\n    Returns:\n        int | float: The sum of `a` and `b`.\n    \"\"\"\n    return a + b\n\ndef is_even(number):\n    \"\"\"Checks if a given number is even.\n\n    Args:\n        number (int): The number to check.\n\n    Returns:\n        bool: True if the number is even, False otherwise.\n    \"\"\"\n    return number % 2 == 0\n\ndef factorial(n):\n    \"\"\"Calculate the factorial of a non-negative integer.\n\n    The factorial of a non-negative integer `n` is the product of all positive\n    integers less than or equal to `n`. By definition, the factorial of 0 is 1.\n    This function computes the factorial iteratively.\n\n    Args:\n        n (int): The non-negative integer for which to calculate the factorial.\n\n    Returns:\n        int: The factorial of `n`.\n    \"\"\"\n    result = 1\n    for i in range(1, n + 1):\n        result *= i\n    return result", "style": "google"}